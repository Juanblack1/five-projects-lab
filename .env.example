# Five Projects Lab does not need private credentials to run.
# Vite exposes every VITE_* value to the browser, so keep secrets out of these fields.

VITE_PUBLIC_AUTHOR="Your Name"
VITE_PUBLIC_PORTFOLIO_URL="https://github.com/your-user"
